# Copy to .env and fill in. .env is gitignored — never commit real keys.
#   cp .env.example .env
CEREBRAS_API_KEY=your_cerebras_key_here
CYBERWAVE_API_KEY=your_cyberwave_key_here

# Optional: connect to an exact twin by UUID (dashboard -> twin -> Copy UUID)
CYBERWAVE_TWIN_ID=
